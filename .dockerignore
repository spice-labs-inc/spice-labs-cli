# Keep the build context lean. The Dockerfile COPYs source + pom.xml; everything
# else here is runtime state, IDE/tooling cruft, or local-only artifacts that
# should never enter the image.

# Build artifacts
target/
**/target/
# Allow Dockerfile.retag to COPY the released fat JAR + ancho into the image.
# These are downloaded by publish.yml (not present in normal source builds).
!target/*-fat.jar
!target/ancho.jar
*.class
dependency-reduced-pom.xml

# IDE / tooling
.classpath
.project
.settings/
.idea/
.vscode/
.bsp/
.metals/
*.iml

# OS
.DS_Store
Thumbs.db

# Git
# .git/ is NOT ignored — the git-commit-id-maven-plugin reads it at build
# time to stamp commit/branch info into git.properties. Excluding it makes
# `mvn package` fail with ".git directory is not found!".
.gitignore

# CI / docs (not needed in the image)
.github/
CONTRIBUTING.md
FAQ.md
README.md
LICENSE

# Docker
Dockerfile*
.dockerignore

# Test fixtures & wrapper test harness (not part of the runtime image)
test/
completions/
docker/

# Secrets / local config
.env
.env.local
.env.*.local
credentials.json
*.pem
*.key
config.local.toml
config.local.json
config.local.yaml
